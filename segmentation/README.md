# Segmentation

This directory contains the image segmentation code used in the study.

Suggested subdirectories:
- `color_models/` for segmentation based on color spaces such as HSV, LAB, and HSI
- `thresholding/` for automatic thresholding methods such as Otsu and Kapur

You may place here the scripts used to segment the original mango images before feature extraction.
