# MLP Classification

This directory contains the code for classification based on tabular features using multilayer perceptrons.

Suggested contents:
- `grid_search_mlp.py`
- `train_best_mlp.py`
- `evaluate_mlp.py`

The dataset split is not predefined in the repository. Users must generate their own training and validation partitions.
