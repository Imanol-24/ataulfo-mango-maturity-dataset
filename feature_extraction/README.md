# Feature Extraction

This directory contains the code used to extract quantitative descriptors from segmented images.

Suggested organization:
- `color_features/` for descriptors derived from color models
- `texture_features/` for descriptors such as GLCM and LBP riu2

This stage can be used either to reproduce the study or to generate alternative feature sets for new experiments.
